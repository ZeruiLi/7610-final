# Tango 组件导出说明

此目录用于存放从 NetEase Tango 设计器导出的组件片段（React + TSX）。

建议流程：

1. 在设计器中定义查询表单、结果卡片、空态等物料；
2. 通过“导出源码”功能，将生成的组件复制到此目录，并以命名导出提供；
3. 在 `src/components` 中引用这些导出，便于与手写组件组合；
4. 更新 README，记录导出日期与设计器版本，方便后续同步。

> 注意：保持组件纯粹，无副作用；样式可通过 CSS Modules 或全局样式补充。
